import math

def add(x, y):
    return x + y

def subtract(x, y):
    return x - y
    
def multiply(x,y):
    return x*y

def divide(x, y):
    return x/y
    
def sine(z):
    return math.sin(z)

def cosine(z):
    return math.cos(z)

def tangent(z):
    return math.tan(z)

def cosecant(z):
    return 1/math.sin(z)

def secant(z):
    return 1/math.cos(z)

def cotangent(z):
    return 1/math.tan(z)

def log(x,base):
    return math.log(x,base)

def logbase10(x):
    return math.log10(x)

def naturallog(x):
    return math.log(x,2.718281828)

def calculator():
        print('Select an operation')
        print('1.Add')
        print('2.Subtract')
        print('3.Multiply')
        print('4.Divide')
        print('5.Trigonometry')
        print('6.Logarithm')
        print('7.Natural Logarithm')
        options = '1', '2', '3', '4', '5'
        choice = raw_input('Enter operation: ')
        if choice == '1' or '2' or '3' or '4' or '5':
            while raw_input != choice:
                if choice == '1':
                    x = int(raw_input('First number: '))
                    y = int(raw_input('Second number: '))
                    return add(x,y)
                if choice == '2':
                    x = int(raw_input('First number: '))
                    y = int(raw_input('Second number: '))
                    return subtract(x,y)
                if choice == '3':
                    x = int(raw_input('First number: '))
                    y = int(raw_input('Second number: '))
                    return multiply(x,y)
                if choice == '4':
                    x = int(raw_input('First number: '))
                    y = int(raw_input('Second number: '))
                    return divide(x,y)
                if choice == '5':
                    trigoptions = 'sine', 'cosine', 'tangent', 'cosecant', 'secant', 'cotangent'
                    trigchoice = raw_input('Enter the type of trig function: ')   
                    z = float(raw_input("Enter your number: "))
                    if trigchoice == 'sine' or 'sin':
                        return sine(z)
                    if trigchoice == 'cosine' or 'cos':
                        return cosine(z)
                    if trigchoice == 'tangent' or 'tan':
                        return tangent(z)
                    if trigchoice == 'cosecant' or 'csc':
                        return cosecant(z)
                    if trigchoice == 'secant' or 'sec':
                        return secant(z)
                    if trigchoice == 'cotangent' or 'cot':
                        return cotangent
                if choice == '6':
                    logchoice = raw_input('Use base 10 or choose your own base?: ')
                    if logchoice == 'Base 10' or 'base 10' or 'Base10' or 'base10':
                        x = float(raw_input('Enter a value: '))
                        return logbase10(x)
                    if logchoice == 'Own' or 'own' or 'Own base' or 'own base':
                        base = float(raw_input('Enter a base: '))
                        x = float(raw_input('Enter a value: '))
                        return log(x,base)
                if choice == '7':
                  x = float(raw_input('Enter a value: '))
                  return naturallog(x)
